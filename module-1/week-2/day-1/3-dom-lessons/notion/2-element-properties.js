// let's read and modify some element's properties with the DOM API

// get one instanceof HTMLElement

const  title = document.getElementById("title-one");

// id
console.log(title.id);
// className
console.log(title.className);
// textContent
console.log(title.textContent);
// innerHTML
console.log(title.innerHTML);

// many other properties are accessible : check MDN
